# 锁屏背景图标透明度调整 + 导入备份文案优化

日期：2026-07-18
分支：dev-harmonyos

## 目标

1. 锁屏遮罩背景装饰品牌图标透明度从 0.08 提升到 0.25，使其清晰可辨但不干扰中心内容。
2. 「我的>导入备份」会员态提示文案从「从本地备份恢复」优化为「从本地存储恢复或快速批量导入」。

## 改动

| 文件 | 位置 | 修改前 | 修改后 |
|---|---|---|---|
| `harmonyos/entry/src/main/ets/pages/Index.ets` | `securityLockOverlay()` 背景装饰 `Image().opacity(...)` | `.opacity(0.08)` | `.opacity(0.25)` |
| `harmonyos/entry/src/main/ets/views/ProfileView.ets:162` | 导入备份会员态提示 | `从本地备份恢复` | `从本地存储恢复或快速批量导入` |

## 不做的事

- 不改变图标数量（6~10 个）、大小（32~56vp）、位置随机逻辑，仅调整透明度数值。
- 不改动「备份数据」项的文案（已在上一轮完成）。

## 验证

`hvigorw assembleApp` 构建通过；真机/模拟器目视确认锁屏背景图标可辨识但不遮挡中心 logo/文字，且导入备份页文案更新正确。
