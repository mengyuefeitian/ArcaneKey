---
phase: 01-fix-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - harmonyos/entry/src/main/module.json5
  - harmonyos/entry/src/main/resources/base/element/string.json
  - harmonyos/entry/src/main/ets/views/ScanView.ets
  - harmonyos/AppScope/app.json5
  - .gitignore
autonomous: false
requirements:
  - FIX-01
  - FIX-02
  - FIX-03

must_haves:
  truths:
    - "module.json5 中含有 ohos.permission.CAMERA 的 requestPermissions 声明"
    - "ScanView.ets 的 doScan() 在调用 startScanForResult 前先请求运行时权限"
    - "AppScope/app.json5 的 bundleName 不含 com.example"
    - "根目录 .gitignore 存在，覆盖 .hvigor/、entry/build/、oh_modules/ 等构建产物"
    - "已跟踪的 177 个构建产物文件从 git 索引中移除"
  artifacts:
    - path: "harmonyos/entry/src/main/module.json5"
      provides: "相机权限静态声明"
      contains: "ohos.permission.CAMERA"
    - path: "harmonyos/entry/src/main/resources/base/element/string.json"
      provides: "camera_reason 字符串资源"
      contains: "camera_reason"
    - path: "harmonyos/entry/src/main/ets/views/ScanView.ets"
      provides: "运行时相机权限申请"
      contains: "requestPermissionsFromUser"
    - path: "harmonyos/AppScope/app.json5"
      provides: "生产级 Bundle ID"
      contains: "com.arcanekey.authenticator"
    - path: ".gitignore"
      provides: "构建产物排除规则"
      contains: ".hvigor"
  key_links:
    - from: "harmonyos/entry/src/main/module.json5"
      to: "系统权限校验"
      via: "requestPermissions 数组"
      pattern: "ohos\\.permission\\.CAMERA"
    - from: "harmonyos/entry/src/main/ets/views/ScanView.ets"
      to: "abilityAccessCtrl.requestPermissionsFromUser"
      via: "doScan() 调用链"
      pattern: "requestPermissionsFromUser"
---

<objective>
修复 Phase 1 的三个关键配置缺陷：
1. FIX-01：双步相机权限修复（module.json5 静态声明 + ScanView.ets 运行时申请）
2. FIX-02：Bundle ID 从 com.example.arcankey 修正为 com.arcanekey.authenticator
3. FIX-03：添加 .gitignore 并移除已跟踪的 177 个构建产物文件

Purpose: 使真机 QR 扫码可用，建立符合 AGC 提交规范的 Bundle ID，清理 git 仓库历史污染。
Output: 已修复的 module.json5、ScanView.ets、app.json5，根目录 .gitignore，干净的 git 工作树。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-fix-audit/01-RESEARCH.md
</context>

<interfaces>
<!-- 以下是执行任务所需的当前代码结构，直接引用，无需重新探索代码库 -->

当前 harmonyos/AppScope/app.json5（完整内容）:
  bundleName: "com.example.arcankey"  →  需改为 "com.arcanekey.authenticator"
  vendor: "example"                   →  需改为 "arcanekey"

当前 harmonyos/entry/src/main/module.json5（完整内容）:
  无 requestPermissions 字段  →  需在 "abilities" 同级添加 requestPermissions 数组

当前 harmonyos/entry/src/main/resources/base/element/string.json（完整内容）:
  "string": [
    { "name": "module_desc", "value": "玄钥身份验证器模块" },
    { "name": "EntryAbility_desc", "value": "玄钥" },
    { "name": "EntryAbility_label", "value": "玄钥" }
  ]
  →  需追加 { "name": "camera_reason", "value": "扫描二维码需要使用相机" }

当前 harmonyos/entry/src/main/ets/views/ScanView.ets 第 1-3 行 import:
  import { common } from '@kit.AbilityKit';
  import { scanBarcode, scanCore } from '@kit.ScanKit';
  →  需追加 import { abilityAccessCtrl, Permissions } from '@kit.AbilityKit';

当前 ScanView.ets doScan() 方法签名（第 49-66 行）:
  private async doScan(enableAlbum: boolean): Promise<void> {
    this.scanning = true;
    try {
      const ctx = getContext(this) as common.UIAbilityContext;
      const options: scanBarcode.ScanOptions = { ... };
      const result: scanBarcode.ScanResult = await scanBarcode.startScanForResult(ctx, options);
      ...
    } catch (e) { ... }
    this.scanning = false;
  }
  →  需在 try 块开头、startScanForResult 调用前插入权限检查逻辑
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1.1: 添加 camera_reason 字符串资源</name>
  <files>harmonyos/entry/src/main/resources/base/element/string.json</files>
  <action>
    向 "string" 数组追加一个条目。module.json5 的 reason 字段引用 "$string:camera_reason"，必须在此处定义，否则编译报错。

    修改后 string.json 完整内容：
    {
      "string": [
        { "name": "module_desc", "value": "玄钥身份验证器模块" },
        { "name": "EntryAbility_desc", "value": "玄钥" },
        { "name": "EntryAbility_label", "value": "玄钥" },
        { "name": "camera_reason", "value": "扫描二维码需要使用相机" }
      ]
    }
  </action>
  <verify>
    <automated>grep -c "camera_reason" harmonyos/entry/src/main/resources/base/element/string.json</automated>
  </verify>
  <done>string.json 中存在 name 为 "camera_reason" 的条目，value 为"扫描二维码需要使用相机"</done>
</task>

<task type="auto">
  <name>Task 1.2: 在 module.json5 中静态声明相机权限</name>
  <files>harmonyos/entry/src/main/module.json5</files>
  <action>
    在 module.json5 的 "module" 对象中，于 "abilities" 数组之后添加 "requestPermissions" 数组。这是 user_grant 类型权限的必须静态声明；若缺少此声明，运行时申请调用将被系统直接拒绝。

    修改后 module.json5 完整内容：
    {
      "module": {
        "name": "entry",
        "type": "entry",
        "description": "$string:module_desc",
        "mainElement": "EntryAbility",
        "deviceTypes": ["phone", "tablet"],
        "deliveryWithInstall": true,
        "installationFree": false,
        "pages": "$profile:main_pages",
        "abilities": [
          {
            "name": "EntryAbility",
            "srcEntry": "./ets/entryability/EntryAbility.ets",
            "description": "$string:EntryAbility_desc",
            "icon": "$media:icon",
            "label": "$string:EntryAbility_label",
            "startWindowIcon": "$media:icon",
            "startWindowBackground": "$color:start_window_background",
            "exported": true,
            "skills": [
              {
                "entities": ["entity.system.home"],
                "actions": ["action.system.home"]
              }
            ]
          }
        ],
        "requestPermissions": [
          {
            "name": "ohos.permission.CAMERA",
            "reason": "$string:camera_reason",
            "usedScene": {
              "abilities": ["EntryAbility"],
              "when": "inuse"
            }
          }
        ]
      }
    }
  </action>
  <verify>
    <automated>grep -c "ohos.permission.CAMERA" harmonyos/entry/src/main/module.json5</automated>
  </verify>
  <done>module.json5 中存在 requestPermissions 数组，包含 ohos.permission.CAMERA 条目，reason 引用 $string:camera_reason</done>
</task>

<task type="auto">
  <name>Task 1.3: 在 ScanView.ets 中添加运行时权限申请</name>
  <files>harmonyos/entry/src/main/ets/views/ScanView.ets</files>
  <action>
    对 ScanView.ets 做两处修改：

    修改一：在第 2 行 import 语句中，将 '@kit.AbilityKit' 的 import 扩展为同时导入 abilityAccessCtrl 和 Permissions：
    将 "import { common } from '@kit.AbilityKit';"
    替换为 "import { common, abilityAccessCtrl, Permissions } from '@kit.AbilityKit';"

    修改二：在 doScan() 方法内，在 "const ctx = getContext(this) as common.UIAbilityContext;" 这行之后，"const options: scanBarcode.ScanOptions" 这行之前，插入以下私有方法调用和权限检查块。

    先在 doScan() 方法之前（即 private async doScan 的上方），添加私有方法 requestCameraPermission：

    private async requestCameraPermission(ctx: common.UIAbilityContext): Promise<boolean> {
      const atManager = abilityAccessCtrl.createAtManager();
      const permission: Permissions = 'ohos.permission.CAMERA';
      try {
        const result = await atManager.requestPermissionsFromUser(ctx, [permission]);
        return result.authResults[0] === abilityAccessCtrl.GrantStatus.PERMISSION_GRANTED;
      } catch (e) {
        return false;
      }
    }

    然后修改 doScan() 方法体，在 ctx 赋值行之后插入权限检查：

    const granted = await this.requestCameraPermission(ctx);
    if (!granted) {
      this.scanErrMsg = '需要相机权限才能扫码';
      setTimeout(() => { this.scanErrMsg = ''; }, 2200);
      this.scanning = false;
      return;
    }

    最终 doScan() 方法结构：
    private async doScan(enableAlbum: boolean): Promise<void> {
      this.scanning = true;
      try {
        const ctx = getContext(this) as common.UIAbilityContext;
        const granted = await this.requestCameraPermission(ctx);
        if (!granted) {
          this.scanErrMsg = '需要相机权限才能扫码';
          setTimeout(() => { this.scanErrMsg = ''; }, 2200);
          this.scanning = false;
          return;
        }
        const options: scanBarcode.ScanOptions = {
          scanTypes: [scanCore.ScanType.QR_CODE],
          enableMultiMode: false,
          enableAlbum: enableAlbum
        };
        const result: scanBarcode.ScanResult = await scanBarcode.startScanForResult(ctx, options);
        if (result?.originalValue) this.parseOtpUri(result.originalValue);
        else { this.scanErrMsg = '未识别到二维码'; setTimeout(() => { this.scanErrMsg = ''; }, 2200); }
      } catch (e) {
        this.scanErrMsg = enableAlbum ? '选择图片失败，请重试' : '扫码失败，请重试';
        setTimeout(() => { this.scanErrMsg = ''; }, 2200);
      }
      this.scanning = false;
    }
  </action>
  <verify>
    <automated>grep -c "requestPermissionsFromUser" harmonyos/entry/src/main/ets/views/ScanView.ets</automated>
  </verify>
  <done>ScanView.ets 中存在 requestCameraPermission 方法，doScan() 在调用 startScanForResult 前先调用该方法，权限拒绝时显示中文错误消息并提前返回</done>
</task>

<task type="auto">
  <name>Task 1.4: 修正 app.json5 中的 Bundle ID</name>
  <files>harmonyos/AppScope/app.json5</files>
  <action>
    将 app.json5 中的两处 example 占位符替换为生产值。bundleName 的 "com.example" 前缀会导致 AGC 提交被拒。

    修改后 app.json5 完整内容：
    {
      "app": {
        "bundleName": "com.arcanekey.authenticator",
        "vendor": "arcanekey",
        "versionCode": 1000000,
        "versionName": "1.0.0",
        "icon": "$media:app_icon",
        "label": "$string:app_name"
      }
    }
  </action>
  <verify>
    <automated>grep -c "com.arcanekey.authenticator" harmonyos/AppScope/app.json5</automated>
  </verify>
  <done>app.json5 中 bundleName 为 "com.arcanekey.authenticator"，vendor 为 "arcanekey"，不含任何 "example" 字样</done>
</task>

<task type="auto">
  <name>Task 1.5: 创建 .gitignore 并移除已跟踪的构建产物</name>
  <files>.gitignore</files>
  <action>
    分两步执行：

    步骤 A：在仓库根目录创建 .gitignore 文件，内容如下：

    # macOS
    .DS_Store
    **/.DS_Store

    # HarmonyOS 构建缓存 (DevEco Studio / hvigor)
    harmonyos/.hvigor/
    harmonyos/entry/build/
    harmonyos/build/

    # ohpm 依赖包 (等同 node_modules，不应提交)
    harmonyos/oh_modules/
    harmonyos/*/oh_modules/

    # IDE 生成文件 (DevEco Studio)
    harmonyos/.idea/.deveco/
    harmonyos/.idea/workspace.xml
    harmonyos/.idea/usage.statistics.xml

    # 本地签名配置
    harmonyos/local.properties

    # HarmonyOS 构建产物
    *.hap
    *.app
    *.har

    # WeChat 小程序构建产物
    miniprogram/miniprogram_npm/

    步骤 B：执行以下 git 命令将已跟踪的构建产物从 git 索引中移除（文件本身保留在磁盘上，仅从版本控制中移除）。执行前先用 git status 确认影响范围，防止误删源码：

    git rm -r --cached harmonyos/.hvigor/ 2>/dev/null || true
    git rm -r --cached harmonyos/entry/build/ 2>/dev/null || true
    git rm -r --cached harmonyos/oh_modules/ 2>/dev/null || true
    git rm -r --cached harmonyos/.idea/.deveco/ 2>/dev/null || true
    git rm --cached harmonyos/.idea/workspace.xml 2>/dev/null || true
    git rm --cached harmonyos/.idea/usage.statistics.xml 2>/dev/null || true
    git rm --cached .DS_Store 2>/dev/null || true
    git rm -r --cached harmonyos/.DS_Store 2>/dev/null || true

    注意：2>/dev/null || true 确保未跟踪的路径不中断执行。
  </action>
  <verify>
    <automated>grep -c ".hvigor" .gitignore && git ls-files harmonyos/.hvigor/ | wc -l</automated>
  </verify>
  <done>.gitignore 文件存在于仓库根目录，git ls-files harmonyos/.hvigor/ 输出为 0（已从索引移除）</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1.6: 人工验证配置修复（含真机验证 FIX-01）</name>
  <what-built>
    已完成：
    1. module.json5 添加了 ohos.permission.CAMERA 的 requestPermissions 声明（含 reason + usedScene）
    2. string.json 添加了 camera_reason 字符串资源
    3. ScanView.ets 添加了运行时权限申请（requestCameraPermission 方法 + doScan() 中的权限检查）
    4. app.json5 Bundle ID 改为 com.arcanekey.authenticator，vendor 改为 arcanekey
    5. 根目录 .gitignore 已创建，已跟踪的构建产物已从 git 索引移除
  </what-built>
  <how-to-verify>
    1. 配置文件验证（文本检查，2 分钟）：
       a. 打开 harmonyos/AppScope/app.json5，确认 bundleName = "com.arcanekey.authenticator"
       b. 打开 harmonyos/entry/src/main/module.json5，确认存在 requestPermissions 数组
       c. 运行：git ls-files harmonyos/.hvigor/ harmonyos/entry/build/ harmonyos/oh_modules/
          → 期望输出为空（0 行），表示构建产物已从索引移除
       d. 运行：git status → 期望 .gitignore 显示为新文件，构建产物目录显示为 untracked

    2. FIX-01 真机验证（需要 HarmonyOS 真机，5 分钟）：
       a. 在 DevEco Studio 中编译并将 App 安装到真机
       b. 进入"扫一扫" Tab，点击"扫描二维码"按钮
       c. 期望：系统弹出相机权限授权弹窗，提示"扫描二维码需要使用相机"
       d. 授权后期望：相机正常打开可扫码
       e. 如果没有弹出授权弹窗而是直接失败，说明 module.json5 的声明格式有误

    注意：FIX-01 只能在真机验证，模拟器不会触发权限弹窗。
    如果当前没有真机，可以先确认配置文件正确后继续 Plan 2，待有真机时再回来验证。
  </how-to-verify>
  <resume-signal>
    真机验证通过后输入 "approved"。
    若无真机暂时跳过真机验证，输入 "skip-device-verify"（将在 FEATURE-AUDIT.md 中标记为待验证）。
    若发现配置问题，描述具体错误内容。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户→相机权限 | 用户可拒绝授权；App 须优雅处理 DENIED 状态 |
| AGC Bundle ID | Bundle ID 变更影响签名证书关联；变更后须重新生成调试证书 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | app.json5 bundleName | accept | 配置文件已纳入版本控制，变更可审计；非 CI 自动化修改 |
| T-01-02 | Denial of Service | ScanView 权限逻辑 | mitigate | 权限拒绝时显示中文错误消息并提前返回，不崩溃 |
| T-01-03 | Information Disclosure | .gitignore 清理 | mitigate | git rm --cached 只移除索引引用，不删除本地文件；不影响已有签名配置 |
| T-01-SC | Tampering | npm/pip/cargo 安装 | accept | 本 Plan 无新增包安装；@kit.AbilityKit 为 HarmonyOS 系统 SDK，无第三方 npm 安装 |
</threat_model>

<verification>
Plan 1 整体验证：

```bash
# 验证 FIX-01 静态声明
grep "ohos.permission.CAMERA" harmonyos/entry/src/main/module.json5

# 验证 FIX-01 运行时申请
grep "requestPermissionsFromUser" harmonyos/entry/src/main/ets/views/ScanView.ets

# 验证 FIX-02 Bundle ID
grep "bundleName" harmonyos/AppScope/app.json5
# 期望输出：bundleName": "com.arcanekey.authenticator"

# 验证 FIX-02 无 example 残留
grep "example" harmonyos/AppScope/app.json5
# 期望输出：无输出（返回非零退出码）

# 验证 FIX-03 .gitignore 存在
test -f .gitignore && echo "exists"

# 验证 FIX-03 构建产物已从索引移除
git ls-files harmonyos/.hvigor/ harmonyos/entry/build/ harmonyos/oh_modules/ | wc -l
# 期望输出：0
```
</verification>

<success_criteria>
- [ ] module.json5 中 requestPermissions 包含 ohos.permission.CAMERA 条目
- [ ] module.json5 中 reason 字段为 "$string:camera_reason"（引用字符串资源，非硬编码）
- [ ] string.json 中存在 camera_reason 条目
- [ ] ScanView.ets 中存在 requestCameraPermission 方法
- [ ] ScanView.ets 中 doScan() 在 startScanForResult 前调用权限检查
- [ ] app.json5 的 bundleName 为 "com.arcanekey.authenticator"
- [ ] app.json5 的 vendor 为 "arcanekey"
- [ ] 根目录 .gitignore 存在
- [ ] git ls-files harmonyos/.hvigor/ 输出为空
- [ ] 真机验证：扫码时弹出相机权限授权弹窗（或标记为待验证）
</success_criteria>

<output>
Create `.planning/phases/01-fix-audit/01-01-SUMMARY.md` when done.

SUMMARY.md 应包含：
- 已修改文件列表及变更摘要
- FIX-01/FIX-02/FIX-03 最终状态
- 真机验证结果（通过 / 跳过 / 失败）
- 如真机验证跳过，记录待验证项供 FEATURE-AUDIT.md 引用
</output>
