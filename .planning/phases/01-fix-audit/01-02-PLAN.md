---
phase: 01-fix-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .planning/FEATURE-AUDIT.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04

must_haves:
  truths:
    - "FEATURE-AUDIT.md 存在，包含 22 个功能项的完整对齐状态"
    - "每个功能项明确标注优先级（P0/P1/P2）和鸿蒙端状态（✅/⚠️/❌）"
    - "所有 P0 缺口均有修复 Phase 归属"
    - "加解密跨平台兼容性有具体测试向量验证结论"
    - "差距项的修复优先级与 REQUIREMENTS.md 中的 Phase 2/3 对应"
  artifacts:
    - path: ".planning/FEATURE-AUDIT.md"
      provides: "功能差异完整清单"
      contains: "FEATURE-AUDIT"
  key_links:
    - from: ".planning/FEATURE-AUDIT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Phase 归属标注"
      pattern: "Phase [123]"
---

<objective>
对照微信小程序功能基线，逐项核查鸿蒙端实现状态，输出 FEATURE-AUDIT.md 功能差异文档。
同时执行 AUDIT-02（QR 扫码状态确认）、AUDIT-03（会员功能行为对比）、AUDIT-04（加解密跨平台兼容性验证）。

Purpose: 建立 Phase 2 功能补全的工作清单；为 Phase 3 AGC 集成明确范围边界。
Output: .planning/FEATURE-AUDIT.md，包含 22 个功能项的完整对齐状态矩阵。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-fix-audit/01-RESEARCH.md
@.planning/phases/01-fix-audit/01-01-SUMMARY.md
</context>

<interfaces>
<!-- 审计所需的代码库位置（直接引用，无需重新探索） -->

微信小程序关键文件：
  miniprogram/pages/index/index.js   — 所有页面逻辑
  miniprogram/app.js                 — globalData（tokens、theme、isMember、FREE_TOKEN_LIMIT）
  miniprogram/utils/crypto.js        — encryptData / decryptData
  miniprogram/utils/totp.js          — totp / timeLeft / fmtCode

鸿蒙关键文件：
  harmonyos/entry/src/main/ets/pages/Index.ets             — 主入口（所有 @State、模态框、doBackup/doImport/cloudBackup 等）
  harmonyos/entry/src/main/ets/views/HomeView.ets          — 主页视图（Token 列表、搜索）
  harmonyos/entry/src/main/ets/views/ProfileView.ets       — 个人页（备份、会员、登出）
  harmonyos/entry/src/main/ets/utils/TOTP.ets              — TOTP 算法
  harmonyos/entry/src/main/ets/utils/CryptoUtil.ets        — encryptData / decryptData
  harmonyos/entry/src/main/ets/utils/StorageUtil.ets       — 本地存储 + 会员持久化
  harmonyos/entry/src/main/ets/model/Token.ets             — Token 接口 + THEMES + INITIAL_TOKENS

审计预填结论（来自 01-RESEARCH.md，逐项核实确认）：
  已确认对齐（研究阶段已验证）：TOTP 生成、Token CRUD、Token 搜索、OTP 复制、倒计时环、
    手动输入、10 种主题色、会员系统（演示）、文件备份、文件导入、密钥可见性、差异化删除结构、
    底部导航、Toast、反馈（mailto:）、Logo 品牌色
  已知差距：QR 扫码权限（Plan 1 已修复）、云备份真实性（Phase 3）、加解密跨平台兼容性（待验证）
  不做对齐：分享功能（微信专属）
</interfaces>

<tasks>

<task type="auto">
  <name>Task 2.1: 读取源码确认审计基线</name>
  <files>（只读，不修改）</files>
  <action>
    按顺序读取以下文件，提取审计所需的关键信息：

    1. 读取 miniprogram/utils/crypto.js：
       - 提取 encryptData / decryptData 的算法细节（salt 长度、迭代次数、XOR key 长度）
       - 记录加密格式头（如 "ENC2:" 前缀）

    2. 读取 harmonyos/entry/src/main/ets/utils/CryptoUtil.ets：
       - 提取对应的算法参数
       - 与小程序 crypto.js 逐参数对比

    3. 对比两端加解密算法：
       - salt 生成方式是否一致
       - PBKDF2 迭代次数是否一致（期望均为 10000）
       - XOR key 生成方式是否一致
       - 输出格式（base64 + 分隔符）是否兼容
       - 如发现参数差异，记录为 AUDIT-04 结论中的具体差异项

    4. 读取 miniprogram/app.js：
       - 确认 FREE_TOKEN_LIMIT = 5
       - 确认 MEMBERSHIP_PRICE = 19.90
       - 确认 INITIAL_TOKENS 结构

    5. 读取 harmonyos/entry/src/main/ets/model/Token.ets：
       - 确认 FREE_TOKEN_LIMIT = 5
       - 确认 MEMBERSHIP_PRICE = 19.90
       - 确认 INITIAL_TOKENS 结构

    读取完成后，在内存中整理出所有 22 个功能项的最终状态，用于 Task 2.2。
    如发现任何与研究阶段预填结论不符的情况，以本次代码读取结果为准。
  </action>
  <verify>
    <automated>grep -c "encryptData" miniprogram/utils/crypto.js && grep -c "encryptData" harmonyos/entry/src/main/ets/utils/CryptoUtil.ets</automated>
  </verify>
  <done>两端加解密源码已读取，AUDIT-04 结论已确定（对齐 / 存在差异+具体差异项）</done>
</task>

<task type="auto">
  <name>Task 2.2: 生成 FEATURE-AUDIT.md</name>
  <files>.planning/FEATURE-AUDIT.md</files>
  <action>
    基于 Task 2.1 的代码读取结果和 01-RESEARCH.md 的预填审计表，在 .planning/ 目录下创建 FEATURE-AUDIT.md。

    文件结构要求：
    - 顶部说明：审计日期、审计依据（小程序 main 分支 v1.1）、审计方法
    - 功能矩阵表（见下方要求）
    - P0 缺口摘要章节
    - AUDIT-02/03/04 专项验证结论章节

    功能矩阵表格式（7 列）：
    | 功能 | 优先级 | 小程序状态 | 鸿蒙实现文件 | 鸿蒙状态 | 差异说明 | 修复 Phase |

    状态标记规范：
    - ✅ 对齐 — 功能实现一致，无需修复
    - ✅ 可接受差异 — 实现方式不同但功能语义等价（如 mailto: vs 云函数）
    - ⚠️ 差异 — 功能存在但行为不一致，需修复
    - ❌ 缺失 — 功能未实现
    - N/A — 不适用（明确不做）

    必须覆盖的 22 个功能项（按研究阶段审计表，用 Task 2.1 的最新代码读取结果更新）：
    1. TOTP 代码生成（30s 轮转）
    2. 多 Token 管理（增删改查）
    3. Token 搜索
    4. OTP 复制
    5. 倒计时环（Canvas vs SVG 视觉对比说明）
    6. QR 扫码自动添加 Token（相机）
    7. 从相册选图识别
    8. 手动输入 Token
    9. 10 种主题色
    10. 会员系统（¥19.90/年，限 5 个 Token）
    11. 文件备份（XOR 加密导出）
    12. 文件导入（XOR 解密）
    13. 跨平台加解密兼容性
    14. 密钥可见性切换（会员）
    15. 差异化删除（本地/云）
    16. 云备份/恢复（会员，wx.cloud vs AGC）
    17. 每小时自动同步
    18. 登录（微信 getPhoneNumber vs 鸿蒙设备账号）
    19. 底部导航（浮动胶囊）
    20. Toast 通知
    21. 反馈功能
    22. 分享功能（v1.1）

    P0 缺口摘要章节内容要求：
    - 列出所有 P0 功能中状态非 ✅ 的项
    - 每项标注：当前状态、根本原因、修复 Phase
    - 如 Plan 1（FIX-01）已修复 QR 扫码权限，标注"已在 Phase 1 Plan 1 修复"

    AUDIT-04 专项验证章节要求：
    - 列出两端 encryptData/decryptData 关键参数对比表
    - 给出兼容性结论：完全兼容 / 有差异（列出差异参数）
    - 如有差异：标注是否影响跨平台备份文件互通，以及修复建议

    AUDIT-02/03 确认章节：
    - AUDIT-02（QR 扫码）：记录 FIX-01 修复内容，标注"真机验证状态"（来自 01-01-SUMMARY.md）
    - AUDIT-03（会员功能）：对比两端 FREE_TOKEN_LIMIT、MEMBERSHIP_PRICE、限制逻辑触发点，给出对齐结论
  </action>
  <verify>
    <automated>test -f .planning/FEATURE-AUDIT.md && grep -c "P0\|P1\|P2" .planning/FEATURE-AUDIT.md</automated>
  </verify>
  <done>
    .planning/FEATURE-AUDIT.md 存在，满足以下条件：
    - 包含所有 22 个功能项
    - 每项有明确状态标记（✅/⚠️/❌/N/A）
    - P0 缺口摘要章节存在
    - AUDIT-02/03/04 专项结论章节存在
    - 跨平台加解密兼容性有明确结论（对齐或具体差异）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 审计文档→规划决策 | FEATURE-AUDIT.md 作为 Phase 2 规划依据，结论错误会导致后续 Phase 遗漏修复项 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | FEATURE-AUDIT.md | accept | 文档为规划产出物，不含敏感信息；纳入版本控制可审计 |
| T-02-02 | Tampering | 加解密跨平台兼容性结论 | mitigate | Task 2.1 要求基于代码直接读取，不依赖记忆或假设；若参数不明确，明确标注 [ASSUMED] |
| T-02-SC | Tampering | npm/pip/cargo 安装 | accept | 本 Plan 仅读取文件和写入文档，无任何包安装 |
</threat_model>

<verification>
Plan 2 整体验证：

```bash
# 验证 FEATURE-AUDIT.md 存在
test -f .planning/FEATURE-AUDIT.md && echo "exists"

# 验证包含必要内容
grep -c "TOTP" .planning/FEATURE-AUDIT.md
grep -c "P0" .planning/FEATURE-AUDIT.md
grep -c "AUDIT-04" .planning/FEATURE-AUDIT.md

# 验证所有 22 项功能均有状态标记
grep -c "✅\|⚠️\|❌\|N/A" .planning/FEATURE-AUDIT.md
# 期望 >= 22
```
</verification>

<success_criteria>
- [ ] .planning/FEATURE-AUDIT.md 存在，已提交 git
- [ ] 文档包含全部 22 个功能项，每项有状态标记
- [ ] P0 缺口摘要章节存在，所有 P0 差距有修复 Phase 归属
- [ ] AUDIT-02：QR 扫码权限修复状态已记录（含真机验证结果或待验证标注）
- [ ] AUDIT-03：会员功能对比结论明确（FREE_TOKEN_LIMIT=5、价格、限制逻辑均已对比）
- [ ] AUDIT-04：加解密跨平台兼容性结论明确（完全兼容 / 有差异+具体差异描述）
- [ ] 文档无空状态项（不允许出现"待确认"或"需验证"这类模糊表述，除非注明具体验证路径）
</success_criteria>

<output>
Create `.planning/phases/01-fix-audit/01-02-SUMMARY.md` when done.

SUMMARY.md 应包含：
- FEATURE-AUDIT.md 的关键结论摘要（P0 缺口数量、P1 差距数量、完全对齐项数量）
- AUDIT-02/03/04 结论各一句话
- Phase 2 功能补全的预期工作量（基于审计结论）
- Phase 3 AGC 集成的入口条件是否就绪
</output>
